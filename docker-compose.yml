services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: luther
      POSTGRES_PASSWORD: luther
      POSTGRES_DB: luther
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
